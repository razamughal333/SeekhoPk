// Paste image URLs here — used across the site. Leave empty ('') to fall
// back to the teal/saffron stripe placeholder instead of a broken image.
// Free sources: unsplash.com, pexels.com (right-click a photo > copy image address).

export const HERO_BANNER_URL = '';
// Shows on the Home page, the banner strip between the hero and course list.
// Suggested shape: landscape, roughly 1600x500.

export const AUTH_SIDE_URL = '';
// Shows on Login & Register pages, side panel (desktop only).
// Suggested shape: portrait, roughly 900x1200.
